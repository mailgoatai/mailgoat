---
title: inbox
---

Manage local inbox cache and webhook receiver

## Usage

```bash
mailgoat inbox --help
```

## Arguments

- Query string, e.g. "subject:invoice" or "alice@example.com"

## Common Options

- Show only unread messages
- Filter messages since relative or ISO time (e.g. 1h, 30m)
- Path to inbox SQLite database
- Output result as JSON
- Path to inbox SQLite database
- Output result as JSON
- Host to bind
- Port to listen on
- Webhook path
- Path to inbox SQLite database
- Show only unread messages
- Filter messages since relative or ISO time (e.g. 1h, 30m)
- Path to inbox SQLite database
- Output result as JSON
