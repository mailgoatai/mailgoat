# Poll interval in seconds (default 300)
POLL_SECONDS=300

# Local state file for processed IDs
STATE_FILE=~/.mailgoat-node-agent-state.json

# Tag attached to outbound replies
RESPONSE_TAG=auto-node-agent
