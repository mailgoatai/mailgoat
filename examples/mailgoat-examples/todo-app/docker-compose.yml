services:
  todo-backend:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "cd backend && npm install && npm run dev"
    env_file:
      - .env.example
    ports:
      - "3001:3001"
