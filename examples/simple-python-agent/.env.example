# Seconds between inbox checks (default: 300 = 5 minutes)
POLL_SECONDS=300

# Local state file for processed message IDs
STATE_FILE=~/.mailgoat-simple-agent-state.json

# Prefix for response subject lines
REPLY_PREFIX=[Auto-Reply]

# Optional explicit sender address
MAILGOAT_FROM=
