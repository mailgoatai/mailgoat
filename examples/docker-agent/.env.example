# Cron syntax, default every 5 minutes
CRON_SCHEDULE=*/5 * * * *

# Fallback recipient if incoming message has no from address
TO_FALLBACK=

# Optional state file override
STATE_FILE=/data/docker-agent-state.txt
